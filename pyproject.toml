[project]
name = "apex-analytics"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "rickardgarnau", email = "rickardgarnau@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "dlt[parquet,snowflake]>=1.30.0",
    "pandas>=3.0.5",
]

[project.scripts]
apex-analytics = "apex_analytics:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
